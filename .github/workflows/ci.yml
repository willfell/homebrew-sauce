name: ci

on:
  push:
    branches: [main]
  pull_request:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.event_name == 'pull_request' && github.ref || github.sha }}
  cancel-in-progress: ${{ github.event_name == 'pull_request' }}

jobs:
  formula:
    runs-on: homebrew-sauce
    steps:
      - uses: actions/checkout@v4
      - name: Provide build tools for native gems
        run: |
          set -euo pipefail
          if ! command -v g++ >/dev/null 2>&1; then
            sudo apt-get update -qq
            sudo apt-get install -y -qq build-essential
          fi
      - name: Install Homebrew if missing
        run: |
          set -euo pipefail
          if [ ! -x /home/linuxbrew/.linuxbrew/bin/brew ]; then
            NONINTERACTIVE=1 bash -c "$(curl -fsSL https://raw.githubusercontent.com/Homebrew/install/HEAD/install.sh)"
          fi
      - name: Audit and fetch the formula
        run: |
          set -euo pipefail
          eval "$(/home/linuxbrew/.linuxbrew/bin/brew shellenv)"
          brew tap willfell/sauce "$PWD"
          brew audit --online --skip-style willfell/sauce/sauce
          brew fetch --force willfell/sauce/sauce
  lint:
    uses: willfell/wac.lab.actions/.github/workflows/actionlint.yml@v1.12.0
    with:
      runner: homebrew-sauce
    permissions:
      contents: read
